#pragma once
#include <string>
#include <vector>

#include "Tools.h"

class data
{
public:
	std::vector<std::string> spirits;
	std::vector<std::string> texts;
	//std::vector<>

private:
	void readData(std::string fileName) {
		
	}
};